import { FilterInWaitingListPipe } from './filter-in-waiting-list.pipe';

describe('FilterInWaitingListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInWaitingListPipe();
    expect(pipe).toBeTruthy();
  });
});
